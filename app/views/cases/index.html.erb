<h2>All your Cases with the "<%= @filter == "error with filter" ? @filter : @filter.name %>" filter</h2>

<% if @cases == "error with cases" %>
  An error getting the cases
<% else %>
  <ul>
    <% @cases.entries.each do |a_case| %>
      <li><%= a_case.subject %></li>
    <% end %>
  </ul>
<% end %>